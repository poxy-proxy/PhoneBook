<Window x:Class="PhoneBook.SearchNumberWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoneBook"
        mc:Ignorable="d"
        Title="Поиск по номеру телефона" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="searchTextBox" Grid.Row="0" Margin="10" />

        <Button x:Name="searchButton" Content="Найти" Grid.Row="1" Margin="10" />

        <Button x:Name="resetButton" Content="Сброс" Grid.Row="1" Margin="10,54,10,-34" Click="resetButton_Click" />
    </Grid>
</Window>
